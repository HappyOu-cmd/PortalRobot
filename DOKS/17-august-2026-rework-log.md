# Переработки Август 2026

## 03_08_2026 - 4 часа

- Добавлен компонент «Загрузка оборудования» на главный экран:
  - Станок 1, Станок 2, Станок 3 и Робот.
  - Круговые индикаторы, проценты и горизонтальные прогресс-бары.
  - Станки отображаются зелёным цветом, робот — синим.
  - Данные подключены к тегам `rLoadCNC_1`, `rLoadCNC_2`, `rLoadCNC_3`, `rRobot`.

- Статистика загрузки оборудования прокинута из `GVL_STATISTIC` в `GVL_HMI` и OPC UA.

- Исправлено открытие циклограммы:
  - Панель статистики открывается и закрывается вместе с циклограммой.
  - Добавлена анимация открытия и закрытия.

- Исправлено мигание и повторное масштабирование 3D-модели при открытии циклограммы.

- Исправлен алгоритм расчёта загрузки станков в PLC:
  - Загрузка считается непрерывно во время обработки.
  - Убрана задержка, возникавшая из-за расчёта только по фронту и спаду `Cycle_Active`.

- Исправлена логика определения работы робота в станке:
  - Начало — при переходе `FB_MACHINE` в состояние `xBusy`.
  - Окончание — при появлении `xRobotDone`.

- Добавлена фильтрация переходных состояний между действиями робота для устранения ложных микро-простоев.

- Исправлено объединение действий робота в магазине в одну общую ячейку.

- Настроено отображение простоев станков красным цветом.

- Добавлена кнопка очистки данных циклограммы с алгоритмом:
  - постановка циклограммы на паузу;
  - сброс накопленных данных.

- Добавлена плавная прокрутка и обновление циклограммы в режиме реального времени.

- Реализовано формирование Excel-отчёта циклограммы с диаграммой Ганта и цветами, соответствующими веб-визуализации.

- Исправлено выравнивание строк и действий в Excel-диаграмме Ганта.

- Настроен базовый масштаб веб-циклограммы — 1 минута.

- Ускорена и стабилизирована работа обмена тегами и обработка команд интерфейса.

## 04_08_2026 - 2 часа

- Переработана логика `FB_CELL_MANAGER`: робот заново выбирает доступный станок при отключении или аварии текущей цели.
- Добавлена приоритизация: сначала станки, готовые принять робота, затем станок с минимальным оставшимся временем обработки.
- Учтена проверка альтернативных станков, когда робот уже находится у текущего станка.
- Добавлена защита от постоянного переключения цели при одинаковом времени с мёртвой зоной около 1 секунды.
- Ошибка открытия двери или `TIMEOUT` теперь формирует аварию станка, но не переводит всю ячейку в критическую ошибку; робот выбирает другой станок.
- Уточнена логика включения и отключения станков во время обработки и при аварии.
- Добавлена возможность отменять запросы на отключение станка и линии.
- Для команд HMI добавлены теги `xAllowed`: они только визуально приглушают кнопки, а окончательная проверка выполняется в PLC с формированием предупреждения.
- Разделены сбросы аварий станков и общих ошибок функциональных блоков.
- Предупреждения больше не сбрасываются кнопкой сброса аварий.
- В меню аварий добавлены фильтры: только аварии, только предупреждения или все события.
- Реализовано отображение последнего активного события под верхним Header.
- Все активные ошибки функциональных блоков доступны для отображения, а не только последняя.
- Добавлены отдельные ошибки `TIMEOUT` в `FB_MACHINE`.
- Обновлён `AGENTS.md`: CODESYS запускает и проверяет пользователь; для новых OPC UA-кнопок необходимо предусматривать `xAllowed` и PLC-предупреждения.
- Сформирована концепция расширенного управления роботом: статусы, ручное перемещение, именованные точки, управление захватом и диагностика.

## 06_08_2026 - 2 часа

- Проведён аудит логики `FB_AXIS` и `FB_AXIS_GROUP`.
- Исправлено включение и отключение приводов.
- Исправлен сброс ошибок SoftMotion.
- Разделено управление отдельными осями и группой осей.
- Доработаны шаговый и непрерывный режимы JOG.
- Исправлены HOME и абсолютные перемещения.
- Настроено торможение после отпускания JOG.
- Глобальный коэффициент скорости применён ко всем движениям.
- Исправлено поведение осей около программных лимитов.

## 10_08_2026 - 2 часа

- Выполнен редизайн расширенного управления роботом.
- Доработан редактор глобальной скорости.
- Исправлена работа кнопок Stop и Reset.
- Доработано ручное управление захватами.
- Разрешено управление захватами при авариях и отключённых приводах.
- Исправлена ошибка «Оба захвата заняты при запуске».
- Добавлена остановка робота при аварии обслуживаемого станка.
- Неисправный станок исключается из маршрутизации.
- Добавлена возможность продолжать работу через исправные станки.
- Проведён аудит каскадных ошибок и общей логики сброса.
- Подготовлена классификация аварий по типам реакции.

## 13_08_2026 - 2 часа

- Начата разработка отдельного Python-симулятора контроллера робота SC-500.
- Подготовлен интерфейс локального пульта на PySide6:
  - индикация координат осей и состояния движения;
  - отображение дискретных сигналов, захватов и текущей команды;
  - элементы ручного управления и диагностики обмена.
- Разделены модель робота, визуальный интерфейс и транспортный слой, чтобы симулятор можно было запускать без HMI.
- Уточнена схема обмена OPC UA между PLC, gateway и веб-визуализацией.
- Зафиксирован перечень командных и диагностических тегов для дальнейшей интеграции Modbus-режима.

## 17_08_2026 - 2 часа

- Реализован Modbus TCP Server для симулятора SC-500 с поддержкой чтения и записи holding-регистров.
- Добавлены регистры команд, координат, состояния робота, захватов и диагностики связи.
- Подготовлен механизм подтверждения команд через `CommandSeq` и `AckSeq`, исключающий повторное выполнение одной транзакции.
- Добавлен heartbeat и контроль потери связи между PLC и симулятором.
- Реализован локальный control API для автоматических тестов и аварийных инъекций.
- Добавлена эксклюзивная тестовая сессия с гарантированным освобождением управления после завершения или падения теста.
- Настроен запуск симулятора как Python-модуля и проверено корректное завершение фоновых сервисов.

## 18_08_2026 - 2 часа

- Добавлен второй индексный магазин и выполнено сквозное разделение данных двух магазинов в PLC, HMI и 3D-сцене.
- Для каждого магазина реализованы независимые зоны, ось индексирования, инвентарь заготовок и готовых деталей.
- Расширена команда робота параметрами `uiMagazineId` и `iActiveSlot` для однозначного выбора магазина и позиции.
- Переработан `FB_CELL_MANAGER`:
  - добавлена проверка совместимости типа детали и станка;
  - выбор станка вынесен в отдельные методы;
  - добавлена маршрутизация заготовки, уже находящейся в захвате;
  - старый менеджер ячейки перенесён в архивную область.
- Добавлен выбор режима управления роботом `SoftMotion / Modbus TCP` с общей структурой команд и статусов.
- Конфигурация Modbus и выбранный режим перенесены в RETAIN-переменные.
- Основной цикл `PLC_PRG` разделён на методы чтения HMI, управления механизмами, публикации статусов, аварий и тестового контура.
- Подготовлены карта регистров SC-500, инструкция по вводу контроллера в эксплуатацию и описание Python-симулятора.

## 19_08_2026 - 2 часа

- Доработана модель двух индексных магазинов:
  - уточнена геометрия зон и слотов;
  - добавлены команды HMI для выбранного магазина;
  - исправлено отображение состава магазина в матрице визуализации.
- Настроено сохранение конфигурации симулятора и восстановление параметров после перезапуска.
- Реализован автоматический test runner для проверки полного цикла ячейки.
- Добавлено хранение тестовых сценариев и результатов в SQLite через gateway.
- В PLC добавлены тестовый буфер сценария, наблюдаемые состояния и служебный контур управления тестами.
- Подготовлены сценарии нормального цикла, потери связи, отказа оборудования и корректного завершения тестовой сессии.
- Исправлено представление ошибок оси и публикация диагностического состояния в HMI.

## 20_08_2026 - 2 часа

- Сведены в одну рабочую версию изменения PLC, симулятора, gateway и веб-визуализации.
- Проверены сквозные контракты для двух магазинов, трёх станков и двух режимов управления роботом.
- Уточнена логика распределения нескольких типов деталей по совместимым станкам.
- Добавлены предупреждения для ситуаций, когда заготовку невозможно направить на доступное оборудование.
- Проверена работа автоматического цикла при отключении одного из магазинов или станков.
- Систематизированы структуры PLC: функциональные блоки, GVL, DUT и методы `PLC_PRG` разнесены по областям ответственности.
- Обновлена документация по архитектуре, Modbus-карте, симулятору, тестовому стенду и индексным магазинам.
- Выполнена итоговая синхронизация ветки с основным Git-репозиторием.

## 24_08_2026 - 2 часа

- Доработан семантический журнал событий производственной ячейки.
- В SQLite добавлено хранение:
  - команд оператора;
  - переходов состояний робота, станков и магазинов;
  - аварий и предупреждений;
  - событий подключения и потери связи с PLC.
- Добавлена нормализация повторяющихся событий, чтобы журнал не забивался одинаковыми сообщениями на каждом цикле опроса.
- Сформирована временная последовательность событий для последующей диагностики автоматов PLC.
- Уточнено отображение источника, уровня, текста и времени события в HMI.
- Обновлена документация по структуре базы и правилам использования журнала при расследовании сбоев.

## 25_08_2026 - 2 часа

- Добавлена серверная авторизация пользователей HMI.
- Реализованы SQLite-хранилище учётных записей и cookie-сессии на стороне gateway.
- Добавлены экран входа и панель управления пользователями.
- Разделены права администратора и оператора; управляющие WebSocket-команды блокируются без действующей сессии.
- Режим просмотра телеметрии оставлен доступным без права отправки команд в PLC.
- Подготовлена основа экранов производственной статистики и клиент обмена статистическими данными.
- Доработаны всплывающие панели HMI, наборы шрифтов и визуальная согласованность элементов управления.
- Зафиксировано текущее состояние HMI отдельной контрольной точкой в Git.

## 26_08_2026 - 2 часа

- Реализован расчёт производственной статистики по сменам операторов.
- Добавлено хранение плана, факта, времени авторизации и количества выпущенных деталей в SQLite.
- Физический выпуск детали привязан к подтверждённой укладке готовой детали в магазин, а не к промежуточному состоянию автомата.
- Добавлены панели статистики и обмен данными между gateway и React-клиентом.
- Написаны тесты хранилища статистики и расчёта сменных показателей.
- Переработаны панели симуляции неисправностей и автоматических тестов.
- Для 3D-сцены добавлены рабочие эффекты станков и оборудования, управляемые реальными состояниями PLC.
- Уточнено терминальное состояние выполнения Modbus-команды и его обработка в `FB_ROBOT_MODBUS`.

## 27_08_2026 - 2 часа

- Введена классификация реакции на аварии: `GLOBAL_STOP`, `ROBOT_STOP`, `EQUIPMENT_STOP` и `WARNING`.
- В события аварий добавлен явный эффект, используемый PLC и HMI для определения области остановки.
- Переработан `FB_ALARM_MANAGER`: добавлено получение эффекта события и публикация полной первопричины.
- Уточнено разделение первичных и каскадных ошибок, чтобы одна неисправность не порождала пачку бессмысленных дублей.
- Обновлена логика остановки робота, станков и магазинов в зависимости от типа аварии.
- Расширена публикация статусов оборудования для журнала событий и экрана диагностики.
- Доработано отображение журнала событий в HMI.
- Обновлена документация по авариям, предупреждениям и общей архитектуре ячейки.

## 31_08_2026 - 2 часа

- Доработан менеджер именованных точек робота:
  - уточнено хранение координат;
  - добавлена публикация фактической позиции группы осей;
  - расширена диагностика недоступных и некорректных точек.
- Обновлены значения по умолчанию и инициализация точек при запуске приложения PLC.
- Расширены теги `GVL_HMI` и OPC UA для управления точками, многотипным производством и диагностикой робота.
- Доработан экран расширенного управления роботом и элементы настройки нескольких типов деталей.
- В gateway и React-клиенте добавлена обработка новых статусов, предупреждений и событий PLC.
- Реализованы предупреждения об отклонённых командах и невозможности построить маршрут детали.
- Синхронизированы тексты предупреждений между PLC, журналом событий и `alarm-catalog.json`.
- Заполнены таблицы причин аварий, условий полного сброса и сообщений журнала.
- Добавлен отдельный каталог предупреждений ячейки с условиями их возникновения и снятия.
- Проверена сборка веб-визуализации и обновлена связанная документация.

## Итого

**30 часов.**
