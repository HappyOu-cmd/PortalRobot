# Каталог Smoke, Regression и генеральных тестов ячейки

Документ описывает сценарии, которые реально формирует
`robot_simulator/robot_simulator/test_scenarios.py` и исполняет
`robot_simulator/robot_simulator/test_runner.py` через gateway и настоящий PLC
Runtime.

Под **интеграционными тестами** здесь понимаются наборы `Regression — 73` и
`Генеральные — 8 × 4 партии` из экрана «Сценарии и тесты». Это не Python
unit-тесты и не тесты gateway: они проверяют связку runner → gateway → OPC UA →
PLC → модели оборудования/интерфейс робота.

## Общий критерий PASS

Для каждого корректного сценария runner проверяет:

1. PLC принял начальное состояние (`uiTestScenarioResult = 0`).
2. Фактический инвентарь совпал с заданным.
3. PLC выбрал ожидаемую первую технологическую операцию, станок и магазин.
4. В зависимости от сценария цикл завершился либо был штатно остановлен после
   проверяемого решения.
5. Не возникла неожиданная глобальная ошибка, а число изделий каждого типа во
   всей ячейке не изменилось.

Для намеренно некорректного сценария PASS означает отказ PLC при загрузке
(`uiTestScenarioResult = 2`) без запуска цикла.

Общие входные условия прогона: PLC Runtime находится в `RUN`, ячейка остановлена,
активна тестовая сессия, выбранная среда и интерфейс робота готовы. Runner между
случаями очищает тестовые ошибки, переводит ячейку в чистое состояние и приводит
робот в `HOME_SAFETY`.

## Smoke — 12 основных сценариев

Обозначения: `T1..T3` — тип изделия; `READY` — пустой станок готов; `WORK` —
заготовка обрабатывается; `DONE` — готовая деталь ждёт выгрузки; `OFF` — станок
отключён. Неуказанные слоты магазина пусты.

| № | Тест | Вход | Ожидаемый выход |
| ---: | --- | --- | --- |
| 1 | Загрузить пустой станок | Один тип. С1=`READY/T1`, С2–С3=`OFF`. В М1 слот 1 содержит заготовку T1. Оба захвата пусты. | PLC берёт заготовку из М1, загружает С1 и завершает цикл. Ошибок нет, баланс T1 сохранён. |
| 2 | Выгрузить готовую деталь | Один тип. С1=`DONE/T1`, С2–С3=`OFF`. М1 пуст и имеет свободный слот. Захваты пусты. | PLC выгружает готовую деталь из С1, кладёт её в М1 и завершает цикл. Ошибок нет, баланс сохранён. |
| 3 | Сменить деталь на заготовку | Один тип. С1=`DONE/T1`. В М1 есть заготовка T1 и свободный слот. Захваты пусты. | PLC выполняет смену: выгружает деталь, загружает заготовку, возвращает деталь в М1 и завершает цикл. |
| 4 | Заготовка в захвате 1 и один свободный слот | Один тип. С1=`DONE/T1`. Захват 1 держит заготовку T1, захват 2 пуст; М1 свободен. | После операторского подтверждения PLC выбирает смену на С1. Цикл завершается без ошибки, баланс сохранён. |
| 5 | Вернуть ненужную заготовку | Все станки `OFF`. Захват 1 держит заготовку T1, робот ориентирован на деталь, М1 свободен. | PLC выбирает возврат заготовки в М1. После подтверждения операции runner подаёт штатный Stop; ошибки и потеря изделия отсутствуют. |
| 6 | Готовая деталь в захвате 2 | С1=`READY/T1`, остальные `OFF`. Захват 2 держит деталь T1, робот ориентирован на деталь, М1 свободен. | После операторского подтверждения PLC укладывает деталь в М1 и завершает цикл без ошибки. |
| 7 | Два типа и запрос оператора | Два типа. С1=`READY/T1`, С2=`READY/T2`, С3=`OFF`. Захват 1 держит заготовку T2. | PLC спрашивает содержимое и тип груза, принимает ответы и выбирает совместимый С2. Затем runner штатно останавливает цикл; инвентарь не меняется. |
| 8 | Три типа и совместимый станок | Три типа. С1=`READY/T1`, С2=`READY/T2`, С3=`READY/T3`. Захват 1 держит заготовку T3. | PLC проводит операторскую идентификацию и выбирает С3. Затем штатный Stop без ошибки и изменения инвентаря. |
| 9 | Штатный Stop в безопасной точке | Один тип. С1=`READY/T1`, в М1 есть заготовка T1, захваты пусты. | После начала операции runner подаёт Stop. PLC завершает безопасный логический шаг и возвращает ячейку в Idle без ошибки и потери изделия. |
| 10 | Авария робота и Reset | Один тип. С1=`READY/T1`, в М1 есть заготовка T1. Во время отдельного тестового движения вводится ошибка робота. | PLC назначает владельцем ошибки робот (`uiErrorSource=2`). После снятия причины и Reset авария сбрасывается, робот возвращается в `HOME_SAFETY`, затем основной сценарий останавливается штатно. |
| 11 | Загрузить станок из магазина 2 | М1 отключён и пуст. М2 включён, слот 1 содержит заготовку T1. С1=`READY/T1`, захваты пусты. | PLC выбирает М2 (`uiActiveMagazine=2`), берёт из него заготовку, загружает С1 и завершает цикл без ошибки. |
| 12 | Уложить готовую деталь в магазин 2 | М1 отключён. М2 включён и имеет свободный слот. Захват 2 держит деталь T1. | PLC выбирает М2 и укладывает в него деталь. После подтверждения операции runner штатно останавливает цикл; баланс сохранён. |

## General — 8 длительных сценариев

Общий вход каждого сценария: один тип изделия T1; оба магазина включены и
содержат по 120 заготовок T1; все три станка включены; захваты пусты; робот в
`HOME_SAFETY`. Набор разрешён только в среде `SIMULATION`, поскольку runner сам
имитирует операторскую перезагрузку кассет. `READY` означает пустой готовый
станок, `DONE` — станок с готовой деталью T1.

Общая последовательность:

1. PLC полностью обрабатывает М1-A и без нового Start переключается на М2-A.
2. Пока активен М2, runner штатно выполняет для М1 команды `Clear → Fill → Enable`.
3. После М2-A PLC переключается на М1-B; runner так же перезагружает М2.
4. PLC полностью обрабатывает М1-B, переключается на М2-B и завершает цикл после
   заполнения М2 готовыми деталями.

| № | Тест | Начальное состояние станков | Ожидаемый выход |
| ---: | --- | --- | --- |
| 1 | Генеральный цикл — все три станка загружены | `[DONE, DONE, DONE]` | Четыре партии пройдены в порядке `М1 → М2 → М1 → М2`; в конце в станках остаются 3 готовые детали T1. |
| 2 | Генеральный цикл — все три станка пустые | `[READY, READY, READY]` | Та же последовательность четырёх партий; в конце все станки пусты. |
| 3 | Генеральный цикл — загружен только станок 1 | `[DONE, READY, READY]` | Четыре партии завершены; в конце в станках остаётся 1 готовая деталь T1. |
| 4 | Генеральный цикл — загружен только станок 2 | `[READY, DONE, READY]` | Четыре партии завершены; в конце в станках остаётся 1 готовая деталь T1. |
| 5 | Генеральный цикл — загружен только станок 3 | `[READY, READY, DONE]` | Четыре партии завершены; в конце в станках остаётся 1 готовая деталь T1. |
| 6 | Генеральный цикл — загружены станки 1 и 2 | `[DONE, DONE, READY]` | Четыре партии завершены; в конце в станках остаются 2 готовые детали T1. |
| 7 | Генеральный цикл — загружены станки 1 и 3 | `[DONE, READY, DONE]` | Четыре партии завершены; в конце в станках остаются 2 готовые детали T1. |
| 8 | Генеральный цикл — загружены станки 2 и 3 | `[READY, DONE, DONE]` | Четыре партии завершены; в конце в станках остаются 2 готовые детали T1. |

Для каждого сценария PASS дополнительно требует: прирост счётчика каждого
магазина ровно на 240 укладок; после каждой партии — 120 деталей T1 и ни одной
заготовки в соответствующем магазине; обе перезагрузки выполнены только после
освобождения магазина; захваты в конце пусты; ошибок и новых предупреждений нет;
общий баланс изделий сохранён. Watchdog сбрасывается только при физическом или
технологическом прогрессе и завершает тест с FAIL при зависании автомата.
Следующая конфигурация станков загружается только после PASS всех четырёх партий
текущего сценария. Любой FAIL генерального сценария останавливает весь набор.

## Integration / Regression — 73 сценария

Набор состоит из:

- сценариев 1–12 — весь Smoke из таблицы выше;
- сценариев 13–19 — проверка первичного владельца аварии;
- сценария 20 — отмена предзапускового опроса;
- сценариев 21–73 — фиксированные комбинации генератора с seed `50100`.

### Проверки ошибок и операторского опроса

| № | Тест | Вход | Ожидаемый выход |
| ---: | --- | --- | --- |
| 13 | Cell fault owner | Валидная базовая конфигурация; маска ошибки ячейки `cell=1`. | PLC публикует `uiErrorSource=1`. После очистки и Reset ошибка снята, робот в HOME. |
| 14 | Robot fault owner | Валидная базовая конфигурация; маска робота `robot=1`. | PLC публикует `uiErrorSource=2`. После очистки и Reset ошибка снята, робот в HOME. |
| 15 | Magazine 1 fault owner | М1 включён; его маска ошибки равна `1`. | PLC публикует `uiErrorSource=3`. После очистки и Reset ошибка снята. |
| 16 | Magazine 2 fault owner | М1 отключён, М2 включён; маска ошибки М2 равна `1`. | PLC публикует `uiErrorSource=7`. После очистки и Reset ошибка снята. |
| 17 | Machine 1 fault owner | С1=`READY/T1`; маска ошибки С1 равна `1`. | PLC публикует `uiErrorSource=4`. После очистки и Reset ошибка снята. |
| 18 | Machine 2 fault owner | С1=`OFF`, С2=`READY/T1`; маска ошибки С2 равна `1`. | PLC публикует `uiErrorSource=5`. После очистки и Reset ошибка снята. |
| 19 | Machine 3 fault owner | С1–С2=`OFF`, С3=`READY/T1`; маска ошибки С3 равна `1`. | PLC публикует `uiErrorSource=6`. После очистки и Reset ошибка снята. |
| 20 | Operator cancels prestart identification | Два типа. С1=`READY/T1`, С2=`READY/T2`; захват 1 держит заготовку T2. | PLC открывает предзапусковой опрос. Команда Cancel закрывает его и возвращает ячейку в Idle без глобальной ошибки и изменения инвентаря. |

### Фиксированные generated-комбинации

В таблице `З(...)` — количество заготовок в М1 по типам, `Д(...)` — количество
готовых деталей. М1 включён, М2 отключён и пуст. `R0/R1` — исходная ориентация
робота. Для компактности номера конкретных занятых слотов не перечислены: их
машинным источником остаётся генератор с seed `50100`.

| № | Тест | Вход | R | Ожидаемый выход |
| ---: | --- | --- | :-: | --- |
| 21 | Generated 50100-1 | 3 типа; станки `[OFF/T3, DONE/T2, DONE/T1]`; М1: З(0), Д(0); захваты `[пуст, пуст]` | R0 | Выгрузить деталь из станка; полный цикл, без аварий, баланс сохранён. |
| 22 | Generated 50100-2 | 1 тип; станки `[READY/T1, OFF/T1, OFF/T1]`; М1: З(T1=9), Д(T1=2); захваты `[пуст, пуст]` | R1 | Загрузить станок; полный цикл, без аварий, баланс сохранён. |
| 23 | Generated 50100-3 | 1 тип; станки `[READY/T1, OFF/T1, WORK/T1]`; М1: З(0), Д(T1=1); захваты `[пуст, пуст]` | R1 | Выгрузить деталь из станка; полный цикл, без аварий, баланс сохранён. |
| 24 | Generated 50100-4 | 3 типа; станки `[READY/T2, READY/T3, READY/T1]`; М1: З(T1=1,T2=1,T3=3), Д(T2=5); захваты `[пуст, пуст]` | R0 | Загрузить станок; полный цикл, без аварий, баланс сохранён. |
| 25 | Generated 50100-5 | 1 тип; станки `[OFF/T1, DONE/T1, WORK/T1]`; М1: З(T1=4), Д(T1=4); захваты `[пуст, деталь/T1]` | R1 | Положить деталь в магазин; полный цикл, без аварий, баланс сохранён. |
| 26 | Generated 50100-6 | 2 типа; станки `[WORK/T2, WORK/T2, READY/T1]`; М1: З(T1=6,T2=1), Д(T2=2); захваты `[заготовка/T1, пуст]` | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 27 | Generated 50100-7 | 1 тип; станки `[OFF/T1, READY/T1, WORK/T1]`; М1: З(T1=2), Д(T1=3); захваты `[пуст, пуст]` | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 28 | Generated 50100-8 | 2 типа; станки `[READY/T2, WORK/T2, OFF/T1]`; М1: З(0), Д(T1=2,T2=1); захваты `[заготовка/T2, деталь/T2]` | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 29 | Generated 50100-9 | 1 тип; станки `[OFF/T1, OFF/T1, READY/T1]`; М1: З(0), Д(T1=2); захваты `[пуст, пуст]` | R0 | PLC запрещает старт: доступной задачи нет. Цикл не запускается. |
| 30 | Generated 50100-10 | 1 тип; станки `[OFF/T1, DONE/T1, WORK/T1]`; М1: З(T1=3), Д(0); захваты `[пуст, пуст]`; у слота 120 недопустимый тип `0` | R1 | PLC отклоняет сценарий с `result=2`; цикл не запускается. |
| 31 | Generated 50100-11 | 3 типа; станки `[READY/T1, WORK/T3, READY/T2]`; М1: З(T1=1,T2=1,T3=2), Д(T2=3); захваты `[пуст, пуст]` | R1 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 32 | Generated 50100-12 | 3 типа; станки `[WORK/T3, READY/T1, WORK/T2]`; М1: З(T3=1), Д(T2=1); захваты `[пуст, деталь/T1]` | R1 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 33 | Generated 50100-13 | 1 тип; станки `[OFF/T1, WORK/T1, WORK/T1]`; М1: З(T1=3), Д(T1=4); захваты `[заготовка/T1, пуст]` | R1 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 34 | Generated 50100-14 | 2 типа; станки `[WORK/T1, WORK/T1, READY/T2]`; М1 пуст; захваты `[пуст, деталь/T1]` | R1 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 35 | Generated 50100-15 | 3 типа; станки `[OFF/T2, OFF/T3, OFF/T1]`; М1: З(T1=1,T2=1), Д(T1=1); захваты `[заготовка/T1, пуст]` | R0 | Вернуть лишнюю заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 36 | Generated 50100-16 | 1 тип; станки `[OFF/T1, OFF/T1, DONE/T1]`; М1: З(T1=2), Д(T1=3); захваты `[пуст, деталь/T1]` | R1 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 37 | Generated 50100-17 | 1 тип; станки `[OFF/T1, WORK/T1, OFF/T1]`; М1: З(T1=2), Д(T1=1); захваты `[пуст, пуст]` | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 38 | Generated 50100-18 | 1 тип; станки `[OFF/T1, READY/T1, DONE/T1]`; М1: З(T1=1), Д(T1=1); захваты `[пуст, пуст]` | R0 | Загрузить первый совместимый готовый станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 39 | Generated 50100-19 | 1 тип; станки `[OFF/T1, OFF/T1, WORK/T1]`; М1: З(T1=3), Д(0); захваты `[пуст, пуст]` | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 40 | Generated 50100-20 | 3 типа; станки `[READY/T1, DONE/T3, OFF/T2]`; М1: З(T1=1,T2=3,T3=1), Д(T1=4); захваты `[заготовка/T1, деталь/T1]`; у слота 120 тип `0` | R0 | PLC отклоняет сценарий с `result=2`; цикл не запускается. |
| 41 | Generated 50100-21 | 3 типа; станки `[DONE/T3, READY/T1, OFF/T2]`; М1: З(T2=1,T3=1), Д(T2=1,T3=2); захваты `[заготовка/T3, пуст]` | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 42 | Generated 50100-22 | 2 типа; станки `[DONE/T1, WORK/T1, DONE/T2]`; М1 пуст; захваты `[пуст, пуст]` | R1 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 43 | Generated 50100-23 | 3 типа; станки `[DONE/T3, DONE/T1, WORK/T2]`; М1: З(T1=1,T3=2), Д(T1=1,T2=2,T3=1); захваты `[пуст, деталь/T2]` | R0 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 44 | Generated 50100-24 | 3 типа; станки `[READY/T2, DONE/T3, DONE/T1]`; М1: З(T2=1), Д(0); захваты `[пуст, пуст]` | R1 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 45 | Generated 50100-25 | 2 типа; станки `[OFF/T2, WORK/T1, WORK/T2]`; М1: З(0), Д(T1=1,T2=1); захваты `[пуст, деталь/T1]` | R0 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 46 | Generated 50100-26 | 1 тип; станки `[READY/T1, OFF/T1, WORK/T1]`; М1: З(0), Д(T1=1); захваты `[пуст, пуст]` | R1 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 47 | Generated 50100-27 | 1 тип; станки `[WORK/T1, READY/T1, READY/T1]`; М1: З(T1=6), Д(T1=1); захваты `[пуст, пуст]` | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 48 | Generated 50100-28 | 1 тип; станки `[READY/T1, DONE/T1, WORK/T1]`; М1: З(T1=1), Д(T1=1); захваты `[пуст, пуст]` | R0 | Загрузить первый совместимый готовый станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 49 | Generated 50100-29 | 3 типа; станки `[DONE/T2, OFF/T3, WORK/T1]`; М1: З(T1=3,T2=2), Д(T1=1); захваты `[заготовка/T3, пуст]` | R1 | Вернуть лишнюю заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 50 | Generated 50100-30 | 3 типа; станки `[DONE/T2, READY/T3, WORK/T1]`; М1: З(T2=1), Д(T1=1,T3=2); захваты пусты; у слота 120 тип `0` | R1 | PLC отклоняет сценарий с `result=2`; цикл не запускается. |
| 51 | Generated 50100-31 | 3 типа; станки `[WORK/T3, OFF/T1, READY/T2]`; М1 пуст; захваты пусты | R1 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 52 | Generated 50100-32 | 2 типа; станки `[READY/T1, WORK/T1, OFF/T2]`; М1: З(T2=2), Д(T2=4); захваты пусты | R1 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 53 | Generated 50100-33 | 1 тип; станки `[READY/T1, WORK/T1, READY/T1]`; М1: З(T1=4), Д(T1=3); захваты пусты | R1 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 54 | Generated 50100-34 | 3 типа; станки `[OFF/T3, OFF/T2, READY/T1]`; М1: З(T1=1,T2=2), Д(T1=1,T2=1,T3=3); захваты `[заготовка/T1, пуст]` | R1 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 55 | Generated 50100-35 | 3 типа; станки `[DONE/T3, READY/T2, OFF/T1]`; М1: З(T2=1), Д(0); захваты `[заготовка/T3, пуст]` | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 56 | Generated 50100-36 | 1 тип; станки `[READY/T1, WORK/T1, OFF/T1]`; М1: З(T1=6), Д(T1=3); захваты пусты | R1 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 57 | Generated 50100-37 | 1 тип; станки `[OFF/T1, DONE/T1, DONE/T1]`; М1: З(T1=3), Д(T1=3); захваты пусты | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 58 | Generated 50100-38 | 1 тип; станки `[OFF/T1, READY/T1, READY/T1]`; М1: З(T1=4), Д(T1=2); захваты пусты | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 59 | Generated 50100-39 | 1 тип; станки `[DONE/T1, WORK/T1, OFF/T1]`; М1: З(T1=2), Д(T1=3); захваты `[пуст, деталь/T1]` | R0 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 60 | Generated 50100-40 | 3 типа; станки `[READY/T1, READY/T3, OFF/T2]`; М1: З(T1=1,T2=1), Д(T1=1,T2=2,T3=1); захваты `[пуст, деталь/T3]`; у слота 120 тип `0` | R0 | PLC отклоняет сценарий с `result=2`; цикл не запускается. |
| 61 | Generated 50100-41 | 3 типа; станки `[OFF/T2, WORK/T3, DONE/T1]`; М1: З(T2=1), Д(T1=1,T2=2); захваты `[заготовка/T1, пуст]` | R1 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 62 | Generated 50100-42 | 2 типа; станки `[OFF/T1, OFF/T1, DONE/T2]`; М1: З(T2=2), Д(T2=3); захваты `[заготовка/T2, пуст]` | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 63 | Generated 50100-43 | 1 тип; станки `[WORK/T1, DONE/T1, DONE/T1]`; М1: З(0), Д(T1=1); захваты пусты | R0 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 64 | Generated 50100-44 | 1 тип; станки `[DONE/T1, READY/T1, WORK/T1]`; М1 пуст; захваты `[пуст, деталь/T1]` | R1 | Положить деталь в магазин; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 65 | Generated 50100-45 | 1 тип; все станки `OFF`; М1: З(T1=4), Д(T1=4); захваты пусты | R1 | PLC запрещает старт: доступной задачи нет. Цикл не запускается. |
| 66 | Generated 50100-46 | 1 тип; станки `[READY/T1, READY/T1, OFF/T1]`; М1: З(0), Д(T1=3); захваты пусты | R0 | PLC запрещает старт: нет заготовки для загрузки и нет детали в станке. Цикл не запускается. |
| 67 | Generated 50100-47 | 1 тип; станки `[OFF/T1, DONE/T1, WORK/T1]`; М1: З(T1=1), Д(T1=2); захваты пусты | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 68 | Generated 50100-48 | 3 типа; станки `[DONE/T1, READY/T3, READY/T2]`; М1: З(T1=1,T2=2,T3=1), Д(T2=1); захваты пусты | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 69 | Generated 50100-49 | 3 типа; станки `[DONE/T1, WORK/T3, OFF/T2]`; М1 пуст; захваты пусты | R1 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 70 | Generated 50100-50 | 3 типа; станки `[READY/T2, READY/T3, DONE/T1]`; М1: З(T3=1), Д(T1=1,T2=1,T3=2); захваты `[пуст, деталь/T1]`; у слота 120 тип `0` | R1 | PLC отклоняет сценарий с `result=2`; цикл не запускается. |
| 71 | Generated 50100-51 | 3 типа; станки `[OFF/T3, WORK/T1, OFF/T2]`; М1: З(T2=3,T3=1), Д(T1=3,T3=1); захваты пусты | R0 | Выгрузить деталь из станка; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 72 | Generated 50100-52 | 1 тип; станки `[OFF/T1, WORK/T1, WORK/T1]`; М1: З(T1=3), Д(T1=2); захваты пусты | R0 | Сменить деталь на заготовку; подтвердить решение, затем штатный Stop; баланс сохранён. |
| 73 | Generated 50100-53 | 3 типа; станки `[WORK/T2, READY/T1, DONE/T3]`; М1: З(T1=1,T3=1), Д(T1=1,T3=1); захваты пусты | R0 | Загрузить станок; подтвердить решение, затем штатный Stop; баланс сохранён. |

## Динамический набор Generated

Отдельный режим `Generated` создаёт от 1 до 1000 сценариев по указанному seed.
Одинаковый seed и количество дают одинаковую последовательность. Вход каждого
случая включает 1–3 типа, состояния трёх станков, содержимое первых 1–12 слотов
М1, оба захвата и ориентацию робота.

- Каждый десятый случай намеренно получает недопустимый тип `0` в слоте 120 и
  должен быть отклонён PLC с `uiTestScenarioResult = 2`.
- Для корректных случаев независимый Python-oracle заранее вычисляет ожидаемое
  первое решение PLC.
- Полностью исполняется не более 10% корректных случаев и максимум 20 случаев;
  в остальных runner подтверждает первое решение и подаёт штатный Stop.

## Источник истины и актуализация

### Локальные проверки осмотра оборудования HMI

Команда: `cd visu; npm run test:inspection`. Эти проверки выполняются на
синтетических состояниях и SQLite `:memory:`, без запуска PLC и браузера.
Состав Smoke/Regression для PLC не меняется.

| Фиксированный тест | Входное состояние | Ожидаемый результат |
| --- | --- | --- |
| Исторические DOOR-коды | Аварии станка 4/5/13/18/19, 23, 20, предупреждение W4 и неизвестный код | Люк, отдельная операторская дверь и патрон различаются; W4 и неизвестный код не назначаются люку. |
| Замок роботного люка | Коды 24/25 каждого станка, W24/W25, одновременная авария движения люка и замка, повтор замка в маске состояния и маска без события | В диагностике есть отдельный узел «Замок люка»; аварии 24/25 относятся только к нему, предупреждения остаются общими; ошибка движения люка отделена, повторов нет, маска без события сохраняет привязку к замку. |
| Узлы робота | Авария оси Y, захватов 0/1, поворота 2 и общий код внешнего робота | Соответствующие приводы/захваты выделяются раздельно; общий код остаётся без уточнения узла. |
| Одновременные аварии | Люк и дверь станка 1, повтор люка в маске состояния, событие станка 2 и снятая ошибка патрона | Ровно два активных события своего оборудования, без дублей и снятых аварий. |
| Ячейка статичного магазина | TAKE с номером 12, PUT с номером 5; затем недопустимые номера и CHANGE | Номера переводятся из 1-based; неоднозначный/недопустимый номер не угадывается, ошибка содержимого выделяет кассету. |
| Происхождение сигналов | Активны давление, контроль фаз и кнопка поста при подключённом PLC | Давление/фазы отмечены локальными; существующая кнопка — обратной связью. |
| Принадлежность истории | Коды `2:4` и `12:4`, active/restored, alarm/warning | Последняя авария станка 1 выбирается только по точному префиксу `2:` и моменту активации; робот включает свои источники осей и захватов. |
| Изоляция 3D без рендера | Пост вложен в ограждение, авария одной кнопки, соседний и заранее скрытый объекты; выключение показа через корпус | Видим выбранный пост; окрашена только кнопка; трансформации и исходные материалы сохранены; выключение X-ray возвращает проверку глубины; выход восстанавливает исходную видимость и убирает наложения. |
| Фильтрация SQLite | Активации `2:4`, `12:4`, кнопки шкафа и поздний сброс `2:4` | Фильтр одного/нескольких префиксов не смешивает станок с группой осей и не подменяет время аварии временем сброса. |
| Переходы аварийной кнопки | Кнопка уже нажата при подключении, неизменный снимок, освобождение, повторное нажатие | Первая запись помечена observedOnConnect; дубли отсутствуют; следующие переходы active/restored получают время наблюдения; отсутствующие теги не создают записи. |

Машинным источником состава наборов остаётся
`robot_simulator/robot_simulator/test_scenarios.py`, а фактических проверок —
`robot_simulator/robot_simulator/test_runner.py`. При добавлении, удалении или
изменении сценария этот каталог нужно обновлять в той же правке. Текущий состав:
Smoke — 12, Regression — 73.
