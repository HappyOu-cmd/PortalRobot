# Переработки Сентябрь 2026

## 01_09_2026 - 8 часов

- Добавлено отдельное отслеживание операторской двери станков:
  - дверь публикуется отдельно от люка;
  - открытая дверь исключает станок из автоматической обработки и обслуживания роботом;
  - открытие двери во время обслуживания формирует аварийную остановку станка и робота;
  - обновлены журнал событий, каталог аварий и документация по авариям и предупреждениям.

- В нижней карточке станка разделены состояния двери и люка: «Закрыта» / «Открыта».

- В `FB_MACHINE_SIMULATION` добавлена отдельная симуляция двери; симуляция люка сохранена в прежнем контуре.

- Добавлено ручное управление механизмами станка:
  - дверь;
  - люк;
  - патрон.

- Для ручного управления добавлены проверки:
  - станок должен работать в ручном режиме;
  - станок не должен участвовать в автоматической обработке;
  - перед движением выводится полноэкранное предупреждение об опасности защемления.

- Разделена проверка конфигурации многотипного производства:
  - отдельно проверяется корректность конфигурации типов;
  - отдельно проверяется настройка всех обязательных точек.

- Исправлено выравнивание двух захватов в 3D-сцене: после поворота захват с деталью и захват с заготовкой точно меняются местами.

- Переведено хранение инженерных точек из обычного `RETAIN` в `PERSISTENT RETAIN` контроллера.

- Убраны точки «внутри станка» из редактора и рабочих списков станков 1, 2 и 3; legacy-коды протокола сохранены для совместимости.

- Добавлен защищённый импорт и экспорт точек через кастомное меню HMI:
  - gateway работает только с одной папкой резервных копий;
  - проводник и произвольные пути недоступны из веб-визуализации;
  - формат, версия, размер, индексы, `PointId`, координаты и скорость проверяются на gateway;
  - импорт выполняется последовательными подтверждаемыми транзакциями `SAVE` / `AckSeq`;
  - операции записываются в журнал событий.

- Исправлены перекрытия стрелок прокрутки в меню:
  - стрелки назначаются только самому глубокому активному scroll-контейнеру;
  - отключённые направления не рисуются;
  - стрелки фоновых панелей скрываются под модальными окнами;
  - координаты кнопок ограничены границами экрана.

- Выполнена проверка веб-визуализации:
  - `npm run build` завершился успешно;
  - gateway-тесты: 43 из 43 успешно;
  - компиляция PLC в CODESYS не запускалась, её выполняет пользователь.

## Итого

**8 часов.**
